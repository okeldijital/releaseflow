# PX-304.1 — RFDS Compliance Matrix

## Reference
**Primary Authority:** `/docs/ReleaseFlow Design Standard v1.0.md`

## Compliance Overview

| Requirement | Source | Implementation | Status |
|-------------|--------|----------------|--------|
| Application Shell — persistent frame | PDS-09 SA-002 | `AppShell` wraps all `(app)` routes; root is `h-screen overflow-hidden`. | ✅ Compliant |
| Sidebar Navigation | PDS-09 SA-002 | `Sidebar` (packages/ui/src/navigation/sidebar.tsx) | ✅ Compliant |
| Global Search | PDS-09 SA-002 | `Topbar` search input + ⌘K command palette trigger. | ✅ Compliant |
| User Menu | PDS-09 SA-002 | `Topbar` user avatar dropdown with Administration + Sign out. | ✅ Compliant |
| Organization Switcher | PDS-09 SA-002 | Passed via `topbarChildren` from layout. | ✅ Compliant |
| Notifications | PDS-09 SA-002 | Bell icon with unread badge in `Topbar`. | ✅ Compliant |
| Shell never changes between workspaces | PDS-09 SA-002 | `AppShell` is the single layout for all authenticated workspaces. | ✅ Compliant |
| Screen Header — Screen Title | PDS-09 SA-003 | `Topbar` renders `<h1>` derived from breadcrumb tail. | ✅ Compliant |
| Screen Header — Breadcrumb | PDS-09 SA-003 / PDS-03 | `Breadcrumbs` component follows real-world object hierarchy. | ✅ Compliant |
| Screen Header — Primary / Secondary Actions | PDS-09 SA-003 | Primary actions live on the page hero; secondary actions on the page. Shell reserves Top Bar space without prescribing them. | ✅ Compliant |
| Vertical Rhythm — Header 64px | PDS-09 SA-012 / PDS-13 | `Topbar` uses `h-16` (64px). | ✅ Compliant |
| Vertical Rhythm — spacing between sections | PDS-09 SA-012 | `Page` and `DashboardLayout` use padding/margins aligned to the 8-point grid (`--rf-space-*`). | ✅ Compliant |
| Screen Width — Narrow / Standard / Wide | PDS-09 SA-011 / PDS-13 | `Page` exports `width="narrow|standard|wide"` mapped to `max-w-[640|1024|1280px]`. | ✅ Compliant |
| Sidebar Width — 280px expanded / 72px collapsed | PDS-13 (Layout tokens) / Sprint Brief | `lg:w-[280px]` / `lg:w-[72px]` with `transition-[width] duration-200`. | ✅ Compliant |
| Motion — 200ms expand/collapse | PDS-09 / PDS-13 ML-001 | `motion-safe:lg:duration-200` on desktop width transition. | ✅ Compliant |
| Motion — prefers-reduced-motion | PDS-13 ML-017 | `motion-reduce:transition-none` applied to all shell transitions. | ✅ Compliant |
| Tablet — overlay drawer | PDS-09 / Sprint Brief | `fixed` sidebar with backdrop when `collapsed` is false on `max-width: 1023px`. | ✅ Compliant |
| Tablet — auto-close after navigation | Sprint Brief | `navigate()` triggers `onToggle()` when viewport ≤1023px. | ✅ Compliant |
| Mobile — slide-over drawer | Sprint Brief | Same overlay mechanism; `z-index` and backdrop handle slide-over. | ✅ Compliant |
| Mobile — gesture / overlay dismiss | Sprint Brief | Backdrop click dismisses; pointer swipe ≥80px left-to-right also dismisses. | ✅ Compliant |
| Mobile — Escape closes | Sprint Brief / RFDS Accessibility | `Escape` listener on sidebar calls `onToggle()` on ≤1023px. | ✅ Compliant |
| Desktop — hover does not auto-expand | Sprint Brief | Collapse state changes only via explicit toggle button / ⌘\ shortcut. | ✅ Compliant |
| Tooltips in collapsed mode | Sprint Brief | `Tooltip` component wraps nav items in collapsed desktop state. | ✅ Compliant |
| LocalStorage preference | Sprint Brief | `STORAGE_KEY = 'rf-sidebar-collapsed'` read/written on desktop resize. | ✅ Compliant |
| Skip link | RFDS Accessibility | `sr-only` skip link becomes visible on focus. | ✅ Compliant |
| ARIA labels & roles | RFDS Accessibility | `role="navigation"`, `aria-label`, `aria-expanded`, `aria-controls`, `role="menu"`, etc. | ✅ Compliant |
| Touch targets ≥44px | RFDS Design Ergonomics | All nav buttons use `min-h-[44px]`; toggle button uses explicit `h-7 w-7` with surrounding padding. | ✅ Compliant |
| Content never overlapped on desktop | Sprint Brief | Sidebar is in flex flow (`lg:relative`); content column receives remaining width automatically. | ✅ Compliant |

## Out-of-Scope Verification

The following domains remain untouched:

- Operations Center
- Release Workspace
- Artist Workspace
- Domain logic / operational intelligence
- Services, hooks, repositories
- Routing
- Business calculations
- Deliverables

## Exception Log

No RFDS conflicts were encountered that required deviation. All brief-level specifications (280px/72px widths, 200ms transition, gesture dismiss) are satisfied within the existing token and component architecture.
