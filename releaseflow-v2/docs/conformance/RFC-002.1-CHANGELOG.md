# RFC-002.1 — Changelog

**Date:** 2026-06-29

---

## Release Workspace v1.0.0 → v1.1.0 (Platinum)

### Added
- `computeHealth()` import from `operational-intelligence-service.ts` for canonical 5-level health
- `daysUntil()` import from `operational-intelligence-service.ts` (replaces local duplicate)
- Health pill visual: dot indicator + rounded background pill + label + percentage
- `<Badge>` components for release type and genre (replaces raw `<span>` elements)
- `role="dialog"` note for status interaction (refactored in prior work)

### Changed
- Health state: 3-level (Healthy/Attention/Critical) → 5-level (Excellent/Healthy/Attention/Blocked/Critical)
- Page padding: `px-6 py-6` → `px-5 sm:px-7 pt-8 pb-16`
- Context rail breakpoint: 1280px (`xl`) → 1024px (`lg`)
- Release type display: raw `<span>` → `<Badge>`
- Genre display: raw `<span>` → `<Badge>`

### Removed
- Local `function daysUntil(d: Date)` — replaced by canonical implementation
- Local `healthPillClass` and `healthPillLabel` 3-level ternaries

### Fixed
- DD-001: Canonical health (P0)
- DD-002: Release type badge (P1)
- DD-003: Genre badge (P1)
- DD-005: Health pill visual (P2)
- DD-006: Page padding (P2)
- DD-007: Context rail breakpoint (P2)
- DD-008: Duplicate daysUntil (P0)

### Verified
- TypeScript: 6/6
- Build: 1/1
- Tests: 327 passed
- Backend: 0 changes
