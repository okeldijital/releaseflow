/**
 * BUILD-220F — Legacy schedule list → canonical Calendar path map.
 * Pure data (no React).
 */

export const LEGACY_CALENDAR_REDIRECTS: Record<string, string> = {
  '/schedule': '/calendar/schedule',
};
