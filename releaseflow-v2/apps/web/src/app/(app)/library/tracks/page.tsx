'use client';

import { LibraryModule } from '../_components/library-module';

/** BUILD-220D — /library/tracks */
export default function LibraryTracksPage() {
  return <LibraryModule tab="tracks" />;
}
