'use client';

/**
 * BUILD-220E — Canonical Media Files catalogue.
 * Underlying domain remains Assets.
 */

import { MediaFilesModule } from './_components/media-files-module';

export default function MediaFilesPage() {
  return <MediaFilesModule />;
}
