'use client';

import { CalendarModule } from '../_components/calendar-module';

/** BUILD-220F — /calendar/deadlines */
export default function CalendarDeadlinesPage() {
  return <CalendarModule tab="deadlines" />;
}
