'use client';

import { CalendarModule } from '../_components/calendar-module';

/** BUILD-220F — /calendar/schedule */
export default function CalendarSchedulePage() {
  return <CalendarModule tab="schedule" />;
}
